{# templates/security/login.html.twig #}
{% extends 'base.html.twig' %}
{# ... #}
{% block body %}
<link href="{{ asset('CSS/seccion.css') }}" rel="stylesheet" />
    <div class="login">
    <h2>Iniciar sesión</h2>

    {% if error %}
        <div class="error">Usuario o contraseña incorrecta</div>
    {% endif %}

    <form action="{{ path('login') }}" method="post" id="loginForm">
        <div class="form-group">
            <label for="email">Email:</label>
            <input type="text" id="email" name="email" value="{{ last_username }}" autocomplete="email" />
        </div>
        <div class="form-group">
            <label for="password">Contraseña:</label>
            <input type="password" id="password" name="password" autocomplete="current-password" />
        </div>
    </form>

    <div class="botones">
        <button type="submit" form="loginForm" class="boton1">Iniciar sesión</button>
        <button class="boton2"><a href="{{ path('registro') }}">Registrarse</a></button>
    </div>
    <button class="boton2" onclick="history.back()">Volver</button>
</div>



{% endblock %}